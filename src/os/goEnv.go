
package main

import (
    "fmt"
    "os"
)

/*
 * Complete the 'reverseArray' function below.
 *
 * The function is expected to return an INTEGER_ARRAY.
 * The function accepts INTEGER_ARRAY a as parameter.
 */
func main() {
    fmt.Println(os.Getenv("EDMAR"))
    fmt.Println(os.Environ())
}
